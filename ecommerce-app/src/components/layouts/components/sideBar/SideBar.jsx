import { Box } from '@mui/material';
import React from 'react';

function SideBar() {
  return (
    <Box
      sx={{
        width: '356px',
        minHeight: '1000px',
        bgcolor: 'black',
      }}
    ></Box>
  );
}

export default SideBar;
